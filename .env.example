# User-specific API keys (optional - override application keys in config.yml)
# Only needed if you want to use your own API keys instead of application ones

# Last.fm API (optional - application key may be in config.yml)
LASTFM_API_KEY=

# AcoustID (optional - override application key)
ACOUSTID_API_KEY=
