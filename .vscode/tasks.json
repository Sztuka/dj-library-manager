{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "STEP 0 — Setup: create venv & install deps",
      "type": "shell",
      "command": "/usr/bin/env bash -lc 'python3 -m venv .venv && source .venv/bin/activate && pip install -U pip && pip install -r requirements.txt && pip install -e .'",
      "options": { "cwd": "${workspaceFolder}" },
      "problemMatcher": []
    },
    {
      "label": "STEP 1 — Configure: library paths & metadata sources",
      "type": "shell",
      "command": "${workspaceFolder}/.venv/bin/python -m djlib.cli configure",
      "dependsOn": "STEP 0 — Setup: create venv & install deps",
      "options": { "cwd": "${workspaceFolder}" },
      "problemMatcher": []
    },
    {
      "label": "TOOLS — Setup Beatport credentials",
      "type": "shell",
      "command": "${workspaceFolder}/.venv/bin/python -m djlib.cli setup-beatport",
      "dependsOn": "STEP 0 — Setup: create venv & install deps",
      "options": { "cwd": "${workspaceFolder}" },
      "problemMatcher": []
    },
    {
      "label": "TOOLS — Install Essentia (Homebrew)",
      "type": "shell",
      "command": "/usr/bin/env bash -lc '${workspaceFolder}/.venv/bin/python ${workspaceFolder}/scripts/install_essentia.py'",
      "dependsOn": "STEP 0 — Setup: create venv & install deps",
      "options": { "cwd": "${workspaceFolder}" },
      "problemMatcher": []
    },
    {
      "label": "TOOLS — Check audio env",
      "type": "shell",
      "command": "${workspaceFolder}/.venv/bin/python -m djlib.cli analyze-audio --check-env",
      "dependsOn": "STEP 0 — Setup: create venv & install deps",
      "options": { "cwd": "${workspaceFolder}" },
      "problemMatcher": []
    },
    {
      "label": "WORKFLOW 0 — Sync DJ Libraries & Tags",
      "type": "shell",
      "command": "${workspaceFolder}/.venv/bin/python -m djlib.cli sync-dj-libraries --write",
      "dependsOn": "STEP 0 — Setup: create venv & install deps",
      "options": { "cwd": "${workspaceFolder}" },
      "problemMatcher": []
    },
    {
      "label": "WORKFLOW 0 (dry-run) — Sync DJ Libraries & Tags",
      "type": "shell",
      "command": "${workspaceFolder}/.venv/bin/python -m djlib.cli sync-dj-libraries",
      "dependsOn": "STEP 0 — Setup: create venv & install deps",
      "options": { "cwd": "${workspaceFolder}" },
      "problemMatcher": []
    },
    {
      "label": "WORKFLOW 1 — Scan UNSORTED",
      "type": "shell",
      "command": "${workspaceFolder}/.venv/bin/python -m djlib.cli scan --strict",
      "dependsOn": "STEP 0 — Setup: create venv & install deps",
      "options": { "cwd": "${workspaceFolder}" },
      "problemMatcher": []
    },
    {
      "label": "WORKFLOW 1 (flexible) — Scan UNSORTED (no strict)",
      "type": "shell",
      "command": "${workspaceFolder}/.venv/bin/python -m djlib.cli scan",
      "dependsOn": "STEP 0 — Setup: create venv & install deps",
      "options": { "cwd": "${workspaceFolder}" },
      "problemMatcher": []
    },
    {
      "label": "WORKFLOW 2 — Enrich online",
      "type": "shell",
      "command": "${workspaceFolder}/.venv/bin/python -m djlib.cli enrich-online",
      "dependsOn": "STEP 0 — Setup: create venv & install deps",
      "options": { "cwd": "${workspaceFolder}" },
      "problemMatcher": []
    },
    {
      "label": "WORKFLOW 3 — Export approved tracks",
      "type": "shell",
      "command": "${workspaceFolder}/.venv/bin/python -m djlib.cli apply",
      "dependsOn": "STEP 0 — Setup: create venv & install deps",
      "options": { "cwd": "${workspaceFolder}" },
      "problemMatcher": []
    },
    {
      "label": "WORKFLOW 4 — Analyze audio (Essentia)",
      "type": "shell",
      "command": "${workspaceFolder}/.venv/bin/python -m djlib.cli analyze-audio",
      "dependsOn": [
        "STEP 0 — Setup: create venv & install deps",
        "TOOLS — Check audio env"
      ],
      "options": { "cwd": "${workspaceFolder}" },
      "problemMatcher": []
    },
    {
      "label": "WORKFLOW 5 — ML dataset export",
      "type": "shell",
      "command": "${workspaceFolder}/.venv/bin/python -m djlib.cli ml-export-training-dataset",
      "dependsOn": "STEP 0 — Setup: create venv & install deps",
      "options": { "cwd": "${workspaceFolder}" },
      "problemMatcher": []
    },
    {
      "label": "TESTS — run",
      "type": "shell",
      "command": "${workspaceFolder}/.venv/bin/pytest -q",
      "dependsOn": "STEP 0 — Setup: create venv & install deps",
      "options": { "cwd": "${workspaceFolder}" },
      "problemMatcher": []
    },
    {
      "label": "TESTS — coverage",
      "type": "shell",
      "command": "${workspaceFolder}/.venv/bin/pytest --cov=djlib --cov-report=term-missing",
      "dependsOn": "STEP 0 — Setup: create venv & install deps",
      "options": { "cwd": "${workspaceFolder}" },
      "problemMatcher": []
    },
    {
      "label": "TOOLS — Clear HTTP cache",
      "type": "shell",
      "command": "rm -f ${workspaceFolder}/djlib_http_cache* && echo '✅ HTTP cache cleared (MusicBrainz, Last.fm, Beatport)'",
      "options": { "cwd": "${workspaceFolder}" },
      "problemMatcher": []
    },
    {
      "label": "TOOLS — Clean empty folders in UNSORTED",
      "type": "shell",
      "command": "find ~/Music\\ Unsorted -name '.DS_Store' -delete && find ~/Music\\ Unsorted -depth -type d -empty -print -delete && echo '✅ Empty folders removed from Music Unsorted'",
      "options": { "cwd": "${workspaceFolder}" },
      "problemMatcher": []
    },
    {
      "label": "TOOLS — Audit library duplicates (dry-run)",
      "type": "shell",
      "command": "${workspaceFolder}/.venv/bin/python ${workspaceFolder}/scripts/cleanup_library_dupes.py",
      "dependsOn": "STEP 0 — Setup: create venv & install deps",
      "options": { "cwd": "${workspaceFolder}" },
      "problemMatcher": []
    },
    {
      "label": "TOOLS — Fix library duplicates (move to reject + DJ software cleanup)",
      "type": "shell",
      "command": "${workspaceFolder}/.venv/bin/python ${workspaceFolder}/scripts/cleanup_library_dupes.py --fix --force-same-quality",
      "dependsOn": "STEP 0 — Setup: create venv & install deps",
      "options": { "cwd": "${workspaceFolder}" },
      "problemMatcher": []
    },
    {
      "label": "REVIEW — Open track review UI",
      "type": "shell",
      "command": "${workspaceFolder}/.venv/bin/python -m djlib.cli review",
      "dependsOn": "STEP 0 — Setup: create venv & install deps",
      "options": { "cwd": "${workspaceFolder}" },
      "problemMatcher": []
    },
    {
      "label": "TOOLS — Dedup staging (dry-run, UI stopped)",
      "type": "shell",
      "command": "${workspaceFolder}/.venv/bin/python -m djlib.cli dedup-staging --dry-run",
      "dependsOn": "STEP 0 — Setup: create venv & install deps",
      "options": { "cwd": "${workspaceFolder}" },
      "problemMatcher": []
    },
    {
      "label": "TOOLS — Dedup staging (write, UI stopped)",
      "type": "shell",
      "command": "${workspaceFolder}/.venv/bin/python -m djlib.cli dedup-staging",
      "dependsOn": "STEP 0 — Setup: create venv & install deps",
      "options": { "cwd": "${workspaceFolder}" },
      "problemMatcher": []
    },
    {
      "label": "TOOLS — Dedup staging (dry-run, UI running)",
      "type": "shell",
      "command": "curl -s -X POST \"http://localhost:8899/api/dedup-staging?dry_run=true\" | python3 -m json.tool",
      "options": { "cwd": "${workspaceFolder}" },
      "problemMatcher": []
    },
    {
      "label": "TOOLS — Dedup staging (write, UI running)",
      "type": "shell",
      "command": "curl -s -X POST http://localhost:8899/api/dedup-staging | python3 -m json.tool",
      "options": { "cwd": "${workspaceFolder}" },
      "problemMatcher": []
    }
  ]
}
